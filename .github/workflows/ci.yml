name: CI
on:
  pull_request:
    branches:
    - main
  push:
    branches:
    - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@main
    - name: use node 14
      uses: actions/setup-node@v2
      with:
        version: 14.x
    - run: npm ci
    - run: npm test
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@main
    - name: use node 14
      uses: actions/setup-node@v2
      with:
        version: 14.x
    - run: npm ci
    - run: npm run lint
